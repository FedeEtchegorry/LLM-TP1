# Exercise 1 -- EDA, baselines and figures.
numpy
pandas
scikit-learn
matplotlib

# Exercise 2 -- the Transformer, transfer learning and fine-tuning.
# CPU wheels: install with
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
torch
transformers
sentence-transformers
