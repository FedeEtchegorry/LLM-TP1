# Las corridas de la arquitectura de dos torres.
#
# [DEFAULT] es la arquitectura declarada: lo que ARCHITECTURE.md describe, escrito una
# sola vez. [RUN] es la unica sección, y se edita para correr algo distinto. Para probar
# una variante sin tocar el archivo: --set clave=valor, repetible.
#
# La entrega 1 vive congelada en parameters-v1.txt y parameters-v1-modules.txt.

[DEFAULT]
model              = transformer

# Contrato de eda

text_fields        = title, description, ingredients
categorical_fields = category, allergens
numeric_fields     = price_position

# Torre de texto.

d_model            = 64
n_layers           = 1
n_heads            = 4
ffn_multiplier     = 4
dropout            = 0.1
tokenizer          = wordpiece
keep_brackets      = true
positional         = learned
embedding_norm     = true
pooling            = cls
pooler_projection  = true

# Torre tabular y fusion. x_tab tiene 30 columnas y su ancho no es configurable.

tab_tower          = mlp
tabular_dim        = 16
fusion_head        = mlp

# Protocolo de optimización. El corte es por AP, que es la métrica que se reporta:
# cortar por pérdida elegía una época ~7 antes del pico de AP y castigaba distinto a
# cada configuración. El techo de épocas sube porque con AP el pico llega más tarde.
learning_rate      = 1e-4
weight_decay       = 0.01
epochs             = 150
patience           = 10
early_stopping     = ap
batch_size         = 64
seed               = 1337


# La finalista, congelada antes de abrir el holdout. Repite cada valor en vez de
# heredarlos de [DEFAULT]: si los heredara, editar [DEFAULT] para probar algo movería
# tambien al finalista, que es exactamente lo que congelar tiene que impedir.
# Su digest es d2872011c9c0, el mismo que [RUN] al 7 de septiembre, asi que reusa esas
# tres corridas en lugar de entrenar de nuevo.
[FINAL dos torres]
model              = transformer
text_fields        = title, description, ingredients
categorical_fields = category, allergens
numeric_fields     = price_position
d_model            = 64
n_layers           = 1
n_heads            = 4
ffn_multiplier     = 4
dropout            = 0.1
tokenizer          = wordpiece
keep_brackets      = true
positional         = learned
embedding_norm     = true
pooling            = cls
pooler_projection  = true
tab_tower          = mlp
tabular_dim        = 16
fusion_head        = mlp
learning_rate      = 1e-4
weight_decay       = 0.01
epochs             = 150
patience           = 10
early_stopping     = ap
batch_size         = 64
seed               = 1337

# La seccion de trabajo: se edita para correr cualquier otra cosa.
[RUN]
